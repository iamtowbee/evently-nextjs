import { Rubik, Sora, Outfit } from "next/font/google";

export const rubik = Rubik({
  subsets: ["latin"],
  variable: "--font-rubik",
});

export const sora = Sora({
  subsets: ["latin"],
  variable: "--font-sora",
});

export const outfit = Outfit({
  subsets: ["latin"],
  variable: "--font-outfit",
});
